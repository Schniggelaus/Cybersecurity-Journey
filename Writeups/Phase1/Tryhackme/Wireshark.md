Wireshark is used to capture Network-traffic.

## Filtering Operators

|Operator | Definition|
|---------|-----------|
|`and` or `&&`|and|
|`or` or `||`|or|
|`eq` or `==`|equal|
|`ne` or `!=`|not equal|
|`gt` or `>`|greater than|
|`lt` or `<`|less than|
